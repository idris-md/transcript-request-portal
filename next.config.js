/** @type {import('next').NextConfig} */
const nextConfig = {
//     output: 'export',      // generate static 'out' directory on build
//   images: {
//     unoptimized: true,   // important for static export with Next/Image
//   },
}

module.exports = nextConfig
